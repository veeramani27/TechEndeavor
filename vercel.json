{
  "buildCommand": "npm install --prefix frontend && cd frontend && npm run build && cd ../backend && uv add -r requirements.txt ",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/backend/app/main.py"
    },
    {
      "source": "/(.*)",
      "destination": "/frontend/dist/index.html"
    }
  ]
}
